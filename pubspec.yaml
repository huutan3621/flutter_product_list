name: flutter_product_list
description: "A new Flutter project."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  http: ^1.2.2
  provider: ^6.0.0
  sqflite: ^2.0.0+4
  path_provider: ^2.0.3
  freezed_annotation: ^2.2.0
  json_serializable: ^6.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  build_runner: ^2.3.3
  freezed: ^2.2.0

flutter:
  uses-material-design: true
